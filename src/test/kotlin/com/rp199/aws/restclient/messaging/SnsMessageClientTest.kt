package com.rp199.aws.restclient.messaging

internal class SnsMessageClientTest {
/*

    @MockK
    private lateinit var amazonSNS: AmazonSNS

    @InjectMockKs
    private lateinit var snsMessageClient: SnsMessageClient

    @BeforeEach
    fun setUp() = MockKAnnotations.init(this)

    @Test
    fun `Should send message with message attributes`() {
        //Given
        val topicName = "my-topic"
        val message = "Greetings humble human"
        val messageAttributeMap = mapOf("someAttribute" to "some attribute Value",
                "anotherAttribute" to "another value")

        every { amazonSNS.listTopics(any<ListTopicsRequest>()) }
        //When
        val result = snsMessageClient.sendMessage(topicName, message, messageAttributeMap)

        //Then
*/
    
}